import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const inventory = sqliteTable("inventory", { id:integer("id").primaryKey({autoIncrement:true}), code:text("code").notNull().unique(), name:text("name").notNull(), brand:text("brand").notNull(), category:text("category").notNull(), color:text("color").notNull().default(""), quantity:integer("quantity").notNull().default(0), costPrice:real("cost_price").notNull(), sellingPrice:real("selling_price").notNull(), reorderLevel:integer("reorder_level").notNull().default(3), createdAt:text("created_at").notNull() });
